/** Map of gamepad indexes and their connected state. */
export type GamepadMap = Record<number, boolean>

/** Map of gamepad button IDs and pressed state. */
export type ButtonState = Record<number, boolean>
